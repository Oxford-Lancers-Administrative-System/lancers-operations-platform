/**
 * Every word Questionnaire B says — LAN-206, moved to its own route by
 * LAN-343. The copy is the fidelity mockup's, unchanged; it lived in the
 * answer link's `presentation.ts` only because the two shared a route.
 */

export const PRIVACY_NOTE =
  "This secure page records only your response. Other players’ responses are never visible.";
