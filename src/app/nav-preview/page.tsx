import PreviewShell from "./preview-shell";

/**
 * LAN-195 — a running mockup of the hamburger-and-drawer replacement for the
 * operate shell's phone bottom bar. Read `./README.md` before treating
 * anything on this route as authoritative.
 */
export default function NavPreviewPage() {
  return <PreviewShell />;
}
