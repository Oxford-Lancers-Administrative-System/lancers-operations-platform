/**
 * LAN-195 — synthetic values for this mockup only.
 *
 * Never a real record, per the roster-fidelity mockup's own rule: this route
 * carries no authorization and must never carry a real operator's name.
 */
export const SIGNED_IN_OPERATOR = "Preview Operator";
