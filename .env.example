# =============================================================================
# Environment template — copy to .env.local and fill in.
#
#   cp .env.example .env.local
#   npm run db:start        # prints the local URL and keys
#   npm run db:status       # prints them again at any time
#
# NEVER put real values in this file. NEVER commit .env.local.
#
# Three tiers, and the boundary between them matters:
#
#   LOCAL          Generated by the Supabase CLI on your machine. Disposable.
#   BROWSER-SAFE   NEXT_PUBLIC_* — compiled into the client bundle. Assume the
#                  whole internet can read these. Always constrained by RLS.
#   SERVER-ONLY    Privileged. Bypasses RLS. Read from Secret Manager at runtime
#                  in Cloud Run. A production server-only key must never exist on
#                  a development machine, in this repo, in GitHub, in Notion, in
#                  a prompt, or in a client bundle.
# =============================================================================

# ---------------------------------------------------------------------------
# BROWSER-SAFE (local values)
# ---------------------------------------------------------------------------
# Local Supabase API URL. `npm run db:status` -> "API URL".
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Browser-safe key, subject to RLS.
# Supabase is mid-migration between two naming schemes; supply whichever your
# stack prints. The app prefers PUBLISHABLE and falls back to ANON.
#   current: `npm run db:status` -> "publishable key" (sb_publishable_...)
#   legacy:  `npm run db:status` -> "anon key"        (a JWT)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Where "Contact the club" writes to, on the signed RSVP page's terminal screen
# (LAN-79). Browser-safe: it is a published contact address, not a secret.
#
# UNDECIDED. Brian deferred the club's contact address on 14 August 2026, and
# there is deliberately NO fallback in the code — a plausible-looking
# placeholder renders an action that silently fails, which is how the first
# version shipped a `mailto:` to a reserved `.example` domain. While this is
# empty the terminal screen renders no contact button; the copy already tells
# the holder to request a new link from the club.
#
# NEXT_PUBLIC_* is inlined at BUILD time, so setting this on a running Cloud Run
# revision does nothing. Supplying it means adding it to the image build — see
# the pull request's Production handoff.
NEXT_PUBLIC_CLUB_CONTACT_EMAIL=

# ---------------------------------------------------------------------------
# SERVER-ONLY (local values)
# ---------------------------------------------------------------------------
# Privileged key. BYPASSES RLS. Used only by local admin scripts.
# Do not prefix with NEXT_PUBLIC_. Do not point this at production.
#   current: `npm run db:status` -> "secret key"        (sb_secret_...)
#   legacy:  `npm run db:status` -> "service_role key"  (a JWT)
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# The service layer's direct PostgreSQL connection. BYPASSES RLS.
#
# This is a SECOND privileged credential, not the same one as the key above.
# The key above is presented to the Data API, which connects as `authenticator`
# and switches to `service_role`. This is a PostgreSQL login in its own right,
# with whatever attributes and grants that login has — locally, `postgres`,
# which owns every table and has admin privileges. It exists because real
# `begin`/`commit` needs a real connection; see
# docs/adr/0014-transactional-data-access.md.
#
# ON YOUR MACHINE THIS IS LOOPBACK OR NOTHING, and that is enforced:
# src/lib/db/url.ts refuses any non-loopback host and any hosted Supabase
# connection string, unconditionally. Leave this blank and the local default is
# used.
#   `npm run db:status` -> "DB URL"   (postgresql://postgres:postgres@127.0.0.1:54322/postgres)
#
# THE HOSTED VALUE IS NOT A LOCAL VALUE. Since ADR 0026 the deployed Cloud Run
# revision does open one hosted database — as `app_runtime`, a least-privilege
# login, through the shared pooler in transaction mode. That is decided, and it
# changes nothing here: pasting the hosted connection string into .env.local is
# still refused, by the same guard, for the same reason. Only the deployed
# service may open it, only when Cloud Run identifies itself, and only for the
# one target pinned in src/lib/db/runtime-target.ts.
#
# The hosted value lives in Secret Manager as `database-url` and is injected
# into the revision at runtime. It must never exist on a development machine, in
# this repository, in Linear, in a log, or in a prompt.
DATABASE_URL=

# The club link's signing key — LAN-157, D81.
#
# An operator issues one link per event and shares it with the squad; anyone
# holding it sees who was asked, what they said and who turned up, without an
# account. The link is signed rather than guessable: the token is
# HMAC-SHA256(this value, "club-link:v1:<event>:<link row>"), and the database
# stores only its SHA-256 digest, so this is the only thing that can produce a
# valid link.
#
# A missing value is a REFUSAL, not a default. The share control says the
# deployment cannot issue a link and names this setting; nothing is created and
# no link is signed with a fallback, because a fallback would mint links that
# stopped opening the moment a real key was configured.
#
# At least 32 characters. `openssl rand -hex 32` is the shape.
#
# LOCAL: `npm run db:start` generates one per machine and writes it here for
# you. There is nothing to fill in.
#
# HOSTED: it belongs in Secret Manager and is injected into the revision at
# runtime, like `database-url`. **Rotating it invalidates every club link
# already shared** — the tokens are derived from it — so rotate it deliberately
# rather than routinely. It must never exist in this repository, in a commit,
# in Linear, in a log or in a prompt.
CLUB_LINK_SECRET=

# Pool tuning. Left configurable rather than hard-coded so the hosted revision
# can be tuned against real connection limits without a code change; the
# connection TARGET is deliberately not tunable that way. Blank uses the working
# local defaults (10 / 10000 / 5000). Hosted uses 5, which across three
# instances is 15 client connections — see docs/deployment.md.
DATABASE_POOL_MAX=
DATABASE_IDLE_TIMEOUT_MS=
DATABASE_CONNECT_TIMEOUT_MS=

# ---------------------------------------------------------------------------
# LOCAL ONLY — the single pre-provisioned test user (`npm run db:seed-user`)
# ---------------------------------------------------------------------------
TEST_USER_EMAIL=test.user@oxfordlancers.local
# Minimum 12 characters. Invent one locally; it is not shared and not a secret
# anyone else needs to know.
TEST_USER_PASSWORD=

# ---------------------------------------------------------------------------
# AUTOMATED DELIVERY — LAN-78
# ---------------------------------------------------------------------------
# Official 1:1 WhatsApp Business Platform messaging, through the Meta Cloud API
# (LAN-92). Read only by src/lib/delivery/config.ts, and never by anything else.
#
# A missing value is a REFUSAL, not a default: an unconfigured deployment
# records a failed, retryable delivery attempt naming the settings that are
# absent — by name, never by value — and sends nothing. Every developer machine
# and every CI run is unconfigured, and that is the expected state.
#
# The two halves resolve independently. Sending needs the first four; receiving
# a callback needs the last two. An outbound-only deployment is legitimate and
# is what the non-production test path is, while LAN-93 still owes a public
# webhook endpoint.

# Where this deployment answers, with no trailing slash. The RSVP link is built
# from it, which is why no host is hard-coded anywhere in the application.
#   local: http://localhost:3010    hosted: https://…
APP_BASE_URL=

# SENDING. The phone-number identifier is not secret; the access token is.
# In Cloud Run the token comes from Secret Manager at runtime and must never
# exist in this repository, in a commit, in Linear, in a log or in a prompt.
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=

# The approved template's name. An event invitation is business-initiated, so
# Meta requires an approved template — free-form text is delivered only inside a
# 24-hour window the recipient opened, which never applies here. The template
# takes four body parameters, in this order: invitee name, event name, when,
# RSVP URL.
WHATSAPP_TEMPLATE_NAME=
WHATSAPP_TEMPLATE_LANGUAGE=

# How many body parameters that template takes — LAN-124. Optional, and the
# default is the one you want.
#
#   invitation  (default)  four parameters, and the only shape carrying an
#                          RSVP link. Leave this unset for the club's template.
#   none                   a template with no parameters, for proving a live
#                          provider path with Meta's pre-approved hello_world
#                          before the club's own template has been approved.
#                          hello_world is en_US, so set the language too.
#
# A message sent as `none` CARRIES NO RSVP LINK. It shows that delivery works
# and shows nothing else. Set it back before anyone relies on an invitation.
WHATSAPP_TEMPLATE_PARAMETERS=

# RECIPIENTS. The only telephone numbers this deployment may send to — LAN-124.
# Required: with this unset, or set to nothing that parses as a number, the
# whole outbound path reports itself unconfigured and sends to nobody. An
# absent allowlist never means "send to everybody"; it is the one setting whose
# absence would otherwise widen what a deployment does.
#
# Comma, semicolon or newline separated. NOT space separated — a space is part
# of how people write numbers, so "+44 7700 900001" is one entry, not three.
# National form is accepted and normalised against DELIVERY_DEFAULT_CALLING_CODE.
#
# WRITE AN INTERNATIONAL ENTRY WITH ITS `+`. An entry outside the default
# calling code and without a `+` is ambiguous — "1202..." could be a country
# code or a national number missing its trunk zero — and is DISCARDED rather
# than guessed at, because on this side of the comparison a guess would grant
# delivery to a real number in some other country. A recipient is read more
# leniently than an entry, deliberately; src/lib/delivery/allowlist.ts says why.
#
# The real values are private and belong in .env.local or Secret Manager, never
# in this file and never in a commit.
DELIVERY_RECIPIENT_ALLOWLIST=

# RECEIVING. Both secret. The app secret verifies X-Hub-Signature-256 on every
# inbound callback; the verify token is echoed during Meta's subscription
# handshake.
WHATSAPP_APP_SECRET=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=

# Optional, with safe non-secret defaults:
#   WHATSAPP_GRAPH_BASE_URL        https://graph.facebook.com
#   WHATSAPP_GRAPH_VERSION         v26.0
#   DELIVERY_DEFAULT_CALLING_CODE  44
WHATSAPP_GRAPH_BASE_URL=
WHATSAPP_GRAPH_VERSION=
DELIVERY_DEFAULT_CALLING_CODE=

# Optional per-message-kind template overrides — LAN-169. Each defaults to the
# club's canonical name declared in src/lib/delivery/templates.ts, which is what
# LAN-168 registers with Meta and generates its manifest from. Set one only when
# a deployment carries a differently-named approved template, which is what a
# sandbox number does. The invitation's name is WHATSAPP_TEMPLATE_NAME above.
#   WHATSAPP_TEMPLATE_REMINDER       lancers_event_reminder
#   WHATSAPP_TEMPLATE_NUDGE          lancers_event_nudge
#   WHATSAPP_TEMPLATE_CHANGE_NOTICE  lancers_event_change_notice
#   WHATSAPP_TEMPLATE_CANCELLATION   lancers_event_cancellation
#   WHATSAPP_TEMPLATE_ESCALATION     lancers_nonresponse_escalation

# ---------------------------------------------------------------------------
# EMAIL — the automated fallback and the third rung of the chase (LAN-169)
# ---------------------------------------------------------------------------
# Resend, per D3/F5 and the 2026-08-17 amendment. Email is not "the backup": it
# is a scheduled step of the ladder (WhatsApp, WhatsApp again, email) AND the
# automatic fallback that carries a message WhatsApp could not. No operator
# sends, routes to, or confirms it.
#
# A deployment may legitimately have WhatsApp and not email while the club's
# sending domain is still being verified. The email path then records a failed,
# retryable attempt naming the missing settings and sends nothing.
#
# EMAIL_API_KEY is a SECRET. It never appears in code, logs, fixtures, commits
# or a client bundle; in production it lives in GCP Secret Manager.
EMAIL_API_KEY=
# The verified sending address, e.g. `Oxford Lancers <events@example.org>`.
EMAIL_FROM_ADDRESS=
# The only addresses this deployment may email. Comma, semicolon or newline
# separated. Required and fails closed, for the same reason
# DELIVERY_RECIPIENT_ALLOWLIST is: an absent allowlist meaning "email anybody"
# would be the one variable whose absence WIDENS what the deployment does — and
# this is the channel nobody presses anything before.
DELIVERY_EMAIL_ALLOWLIST=
# Optional, with a safe non-secret default:
#   EMAIL_API_BASE_URL  https://api.resend.com
EMAIL_API_BASE_URL=
# Optional. Where a reply goes.
EMAIL_REPLY_TO=

# ---------------------------------------------------------------------------
# THE MESSAGING SCHEDULER (LAN-169)
# ---------------------------------------------------------------------------
# A shared secret, and a SECRET. POST /api/scheduler/messaging runs one sweep:
# it raises what is overdue, dispatches what is due, and answers with counts.
# Cloud Scheduler makes that request in the deployed environment; locally
# `npm run messaging:ticker` does.
#
# A missing value is a REFUSAL, never a default. "No token configured, so allow
# everybody" would make this the one variable whose absence widens what the
# deployment does — and this endpoint makes the club send messages.
#
# Any value will do locally. In production it lives in GCP Secret Manager.
SCHEDULER_TRIGGER_TOKEN=

# ---------------------------------------------------------------------------
# LOCAL ONLY — proving the adapter against Meta's test number
# ---------------------------------------------------------------------------
# Both are read ONLY when APP_BASE_URL is a loopback host, parsed rather than
# pattern-matched. In any deployed environment they resolve to "no override,
# template mode" whatever is set here, and src/lib/delivery/config.test.ts
# asserts exactly that. They exist because Meta's test number cannot reach the
# club's synthetic roster — those numbers are in Ofcom's reserved drama range —
# and cannot send free-form text without an open window.
#
# The recipient is a real phone number and therefore real personal data. Keep it
# in .env.local, which is git-ignored, and out of commits, Linear and logs.
WHATSAPP_TEST_RECIPIENT=
# `text` sends free-form text carrying the link; anything else sends the
# approved template. Loopback only.
WHATSAPP_MESSAGE_MODE=

# Redirects every email to one inbox. Loopback only, exactly as
# WHATSAPP_TEST_RECIPIENT is: there is no reading under which a deployed
# revision should silently send somebody else's message to a different mailbox.
EMAIL_TEST_RECIPIENT=

# Recipients the LOCAL DELIVERY SINK refuses, so a delivery failure can be
# reviewed. Comma or space separated, telephone numbers or email addresses.
#
# The sink itself is chosen by runtime detection and NEVER by a flag — a
# deployed runtime cannot reach it however this file is filled in, and
# src/lib/delivery/local-sink.test.ts proves that against a fully populated
# deployed-looking environment. This variable only changes how the sink behaves
# once it has already been selected, which is loopback-only by construction.
DELIVERY_SINK_FAILURES=

# How often the local ticker sweeps, in milliseconds. Default 15000.
MESSAGING_TICK_MS=

# ---------------------------------------------------------------------------
# ADDRESS SEARCH — LAN-115
# ---------------------------------------------------------------------------
# Searchable place/address suggestions for the event venue field. Read only by
# src/lib/venue-search/config.ts, and never by anything else.
#
# There is NO credential here. The one implemented provider — Photon, Komoot's
# open-source OpenStreetMap geocoder — needs no account, no API key and no
# billing relationship, which is why it was chosen: it puts no secret in this
# repository and asks Brian for no cost decision. See
# src/lib/venue-search/photon.ts for the full reasoning, including why
# Nominatim is not an option (its usage policy forbids autocomplete).
#
# A missing value is a REFUSAL, not a default. Leave the provider unset and the
# application performs no address search at all: the venue field stays the free
# text field it has always been and tells the operator to type the venue
# themselves. That is the expected state for every CI run, and for the deployed
# container until Brian chooses to configure one — no deployment reaches a
# third-party service nobody selected.

# The provider to use. `photon` is the only implemented value; anything else,
# including unset, means no address search.
VENUE_SEARCH_PROVIDER=

# Optional. Where that geocoder lives, with no trailing slash. Blank uses the
# free public instance at https://photon.komoot.io, which carries a fair-use
# expectation and no availability guarantee — searches are debounced in the
# browser and every failure falls back to typing the venue by hand. Point this
# at a self-hosted Photon instance if the club's usage ever outgrows fair use.
#   local: VENUE_SEARCH_PROVIDER=photon and leave this blank
VENUE_SEARCH_BASE_URL=

# ---------------------------------------------------------------------------
# RECRUITMENT SIGN-UP GATE — LAN-202
# ---------------------------------------------------------------------------
# Read only by src/lib/services/recruitment-config.ts, and only to decide
# whether the sign-up form's saved page can reveal the WhatsApp community
# group link — never before consent, and never in a message (LAN-202).
#
# There is NO credential here, and this is not something an agent may set for
# real: minting the club's actual WhatsApp community group and its invite link
# is Brian's own action, in WhatsApp, outside this repository. A missing value
# is a REFUSAL, not a placeholder — the saved page still records consent, and
# still says plainly that the link is not live yet, rather than fabricating
# one. That is the expected state for every CI run and for the deployed
# container until Brian configures a real link.
RECRUITMENT_WHATSAPP_GROUP_LINK=
