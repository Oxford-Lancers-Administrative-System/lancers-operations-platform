# LAN-409 — Braces L and Braces R

Taken at `2234707a` against the local production build, through the real login,
at 1440×900 and 375×812 as measured from the browser context. That head is one
merge behind the branch's tip: `main` came in again afterwards for LAN-399's
operator playbook, which touches none of these screens.

`desktop-record-kit.png` and `phone375-record-kit.png` are a membership record
with every section folded except Kit. Braces L reads **Knee - L, Shoulder** and
Braces R reads **Ankle - S**: one player, two braces on one side, which the two
old single-pick slots could not record at all. Both are multi-select fields in
the idiom Formalwear already uses two rows below them, over the same twelve
brace values Clint's sheet carries.

`desktop-board-braces-columns.png` and `phone375-board-braces-columns.png` are
the roster board's Kit group, scrolled to the two columns. They are named
**Braces L** and **Braces R**, they show the set comma-joined, and they are
exactly the width the two Braces columns were. These two were captured with the
board's own horizontal scroll moved to the Kit group — `visual:preflight`
measures the viewport and does not scroll a table — so they are supplementary
to the record's own evidence rather than a substitute for it; the widths are
the same measured 1440 and 375.
