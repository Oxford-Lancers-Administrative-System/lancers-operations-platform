# LAN-411 — Not delivered

Taken at `2234707a` against the local production build, through the real login,
at 1440×900 and 375×812 as measured from the browser context. The seed carries
exactly one deliberate example — Rufus, on Come-and-try flag session — whose
invitation and all three chases were accepted by Meta and have said nothing
since, which is what happens to somebody who has never accepted WhatsApp's
terms.

`desktop-delivery-summary-notice.png` and `phone375-delivery-summary-notice.png`
are the event's delivery page. The four tiles are untouched — Audience,
Delivered, Queued, Failed — and a warning notice under them reads **1 message
was not delivered.**, beside the held-messages notice and in the same idiom. A
fifth count tile was rejected: `MetricRow` lays out two per line at 375 px, so
it would be a lone half-width tile. **Needs attention** is unchanged, and the
silent row does not appear in it.

`desktop-event-board-row.png` and `phone375-event-board-row.png` are the event's
own table. Rufus's Delivery cell reads **Not delivered** in the warning tone,
with **WhatsApp 4 sent** beneath it — the chase is exactly where it was, which
is the point: the flag is advisory and the follow-up is a phone call.

`desktop-follow-ups-row.png` and `phone375-follow-ups-row.png` are the
Follow-ups queue. Rufus's Status reads **Not delivered**, fourth of five in
precedence, with the Last message column still saying what was sent and when
(WhatsApp · Attempted · 5 May, 18:00). Every other row still reads Chasing.
