# Acceptance — W1

- Workflow: W1 — Bring last season's squad in
- Screen IDs: W1-01, W1-02, W1-03, W1-04
- Grounding: screenshots — `application-walked`. Both sides of all four screens
  are photographs of `main@332bc6b` running locally against seeded synthetic
  data, at a browser-measured 1280 and 375. The confirmation table is the
  application's own: each proposal uploads a real CSV into the shipped file
  input, the component's `onChange` submits it, and the rendered table is then
  rewritten into people.
- Verdict: approved
- Brian's exact words: "Workflow 1 approved. Move on to the next one."
- Date: 2026-09-01
- Open feedback: none
- Evidence paths:
  - `workflows/W1-bring-last-seasons-squad-in.md` — the specification
  - `mockups/W1-bring-last-seasons-squad-in.html` — the review page
  - `mockups/shots/shots.json` — sixteen captures, head SHA and proposal hash per screen
  - `mockups/src/W1-0{1,2,3,4}.js` — the proposals, built into `mockups/proposals/`

## Corrections taken before approval

- **Menu wording.** Brian, 2026-09-01: the labels should be "more professional".
  `Add one player by hand` became **Add one player**, and
  `Import last season's squad` became **Bulk import players**. W2's frozen
  inventory name is untouched; only the operator-visible labels changed.

## What the five proposed decisions became

Brian approved the specification as written, which carries its recommended
default for each. All five are now locked; if any was approved by omission
rather than intent, it returns to him rather than being built on.

| Decision                                                                                                            | Now    |
| ------------------------------------------------------------------------------------------------------------------- | ------ |
| The file carries first name, last name and mobile required; personal email, college and matriculation year optional | locked |
| A carried-forward person's own facts are never overwritten by the file                                              | locked |
| An unanswered duplicate refuses its own row and nothing else                                                        | locked |
| Confirming queues the welcomes; it never sends them                                                                 | locked |
| Four-role, not the general-operator floor the surrounding roster surfaces use                                       | locked |

Column order, header names, the size limit, template wording, and whether the
round-tripping export ships here remain delegated to the Mission Lead.
