# Acceptance — W8

- Workflow: W8 — Work the queue and nudge
- Screen IDs: W8-01, W8-02, W8-03
- Grounding: screenshots — `/operate/people/missing`, a real implemented
  operator route, both sides, measured 1280 and 375. The proposals add columns
  and an action to the shipped MUI table, cloning its own cells and buttons.
- Verdict: approved
- Brian's exact words: "That looks fucking awesome. I love it. Cleared."
- Date: 2026-09-02
- Open feedback: none
- Evidence paths:
  - `workflows/W8-work-the-queue-and-nudge.md`
  - `mockups/W8-work-the-queue-and-nudge.html`
  - `mockups/shots/shots.json`, `mockups/src/W8-0*.js`, `mockups/src/_queue-page.js`

## Decisions

| Decision                                                                | Now                                      |
| ----------------------------------------------------------------------- | ---------------------------------------- |
| The surface is Mission 5's shipped missing-data queue                   | locked                                   |
| A nudge sends the person's own compiled ask, never a new message        | locked                                   |
| Batch means several people, each getting their own ask                  | locked                                   |
| Operator nudges are unlimited and outside the cap                       | locked                                   |
| The queue warns when the automated chase is exhausted, never refuses    | locked                                   |
| Last contact, its kind, and the next automated contact are shown        | locked                                   |
| Terminal delivery failure lists the person, sends no email in its place | locked                                   |
| Leaving mid-onboarding drops the person off the queue                   | locked                                   |
| Nothing on this screen fires on a timer                                 | locked                                   |
| The queue defaults to onboarding players only                           | locked at the recommendation — see below |

## The one locked at its recommendation

The shipped queue lists everybody with missing data — the screens show an
inactive member and an active one, and coaches and committee would appear too.
This mission's collection loop is players only, so the specification recommended
defaulting to onboarding players with Mission 5's wider scope still one click
away. Brian approved without separating that row, so it is locked at the
recommendation.

## The dependency this workflow displays

**LAN-93 is a stated dependency, not an option.** The cap counts messages known
to have arrived, so delivery callbacks are what make "the automated chase is
exhausted" a true statement rather than a guess — and `W8-03` is the screen that
puts that sentence in front of an operator.
