# Acceptance — W9

- Workflow: W9 — Follow up with a recruit
- Screen IDs: W9-01, W9-02, W9-03, W9-04
- Grounding: screenshots and code-only
- Verdict: _awaiting review_
- Brian's exact words:
- Date:
- Open feedback: none recorded yet
- Evidence paths:
  - `workflows/W9-follow-up-with-a-recruit.md`
  - `mockups/W9-follow-up-with-a-recruit.html`
  - `mockups/shots/W9-01-*.png`
  - `mockups/shots/W9-02-*.png`
  - `mockups/shots/W9-03-*.png`
  - `mockups/shots/W9-04-*.png`

## How these screens were made

Photographed from the running application at `main@e669331` on the
`mission-m-recruitment-1` lease, at a browser-measured 1280px and a
browser-measured 375px. The proposal is evaluated into the real page — inserted
between real form fields, or replacing the rows of a real card — so both sides
are the same running page differing only by the change: W9-01, W9-03.

Drawn, because no equivalent surface exists on `main`: W9-02, W9-04.
These carry no current side at all; the review page renders the standard's
`New surface, nothing to compare` placard, and their grounding is `code-only`.

## What this mission does not invent

Every WhatsApp message shown is an approved template, because
`src/lib/delivery/config.ts` permits nothing else in production:
_"template is the only production shape"_. No screen shows an operator typing a
message to a recruit, and no screen shows a two-way chat, because neither exists
and neither can.

## What is synthetic, and what is real

Rosalind Penhaligon (`identified`) and Tobias Wrenfield (`engaged`) are really
seeded at the baseline by `8a4239f`. Every other person, message and event
detail is invented in the same synthetic universe. No real person, contact
detail or club record appears anywhere.
