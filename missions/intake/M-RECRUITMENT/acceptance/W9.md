# Acceptance — W9

- Workflow: W9 — Follow up with a recruit
- Screen IDs: W9-01, W9-02
- Grounding: screenshots and code-only
- Verdict: _awaiting review_
- Brian's exact words:
- Date:
- Open feedback: none recorded yet
- Evidence paths:
  - `workflows/W9-follow-up-with-a-recruit.md`
  - `mockups/W9-follow-up-with-a-recruit.html`
  - `mockups/shots/W9-01-*.png`
  - `mockups/shots/W9-02-*.png`

## How these screens were made

Photographed from the running application at `main@e669331` on the
`mission-m-recruitment-1` lease, at a browser-measured 1280px and a
browser-measured 375px. The proposed side is the same page with the proposal
evaluated into its DOM, so the two sides differ only by the change: W9-01.

Drawn on both sides, because no equivalent surface exists on `main`:
W9-02. These are labelled `New surface, nothing to compare` and
their grounding is `code-only`.

## What is synthetic, and what is real

Rosalind Penhaligon (`identified`) and Tobias Wrenfield (`engaged`) are really
seeded at the baseline by `8a4239f` and render with their real seeded facts.
Every other person, message and event detail is invented in the same synthetic
universe. No real person, contact detail or club record appears anywhere.

## Known artifacts of the mockup, not of the design

Recorded once for the whole set: the pinned first column overlays adjacent text
on a scrolled board, and the previously selected navigation item still paints as
selected beside the new one, because React re-renders that subtree after the
mutation and reverts class edits. Neither changes the design under review.
