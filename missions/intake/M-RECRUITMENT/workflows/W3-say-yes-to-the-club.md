# W3 — removed

**Removed on 2026-08-31 on Brian's instruction.** There is no workflow here.

> "W3 doesn't seem to hold anything. It should just be flat-out removed. Don't
> renumber anything." … "At least there are no screens associated with it. It
> seems to have rolled into something else."

He is right on both counts. Earlier the same day he folded W3 into the doors and
`W10`, and its three screens moved to `W10-03`, `W2-03` and `W6-03`. What was
left was a specification with no actor journey of its own and no surface — a
heading over other workflows' work.

## The number is kept deliberately

`W3` stays in the inventory as an empty slot. It is not reused, and `W4` through
`W14` keep the numbers they were frozen with, because Brian asked for exactly
that: _"Don't renumber anything."_ The frozen inventory is also required to be
consecutively numbered from `W1`, so the slot remains rather than the entry being
deleted from the array.

Nothing should be built here, and nothing should be numbered `W3` again.

## Where its five decisions went

`W3` owned five decisions in the coverage map. Every one has a new owner; none
was dropped.

| Decision        | Source             | Now   | Why                                                                                                                                                                                                                         |
| --------------- | ------------------ | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `D3`            | Task 09 brief      | `W10` | The welcome and community-group invite are fired by the machinery, and boundary item 43 gives `W10` what fires the welcome, the ask and the invitations, and on what trigger. The doors that fire it stay `W5`, `W6`, `W7`. |
| `SIGNON-OWNED`  | Owner, 2026-08-31  | `W10` | Channel registration is this mission's and is its own flow; the flow is machinery, and the machinery is administered in `W10`.                                                                                              |
| `SIGNON-LADDER` | Owner, 2026-08-31  | `W10` | The named sequence is the definition of what the club sends and when. Its templates had already moved to `W10-03`.                                                                                                          |
| `AM-presence`   | Authority manifest | `W2`  | Channel presence is a fact about one recruit, read on the recruit's record.                                                                                                                                                 |
| `T08-row8`      | Task 08 brief      | `W2`  | Row 8 channel presence renders on the recruit's record.                                                                                                                                                                     |

**Both `W10` and `W2` homes are proposed and await Brian's word.**

## One open question this removal exposes

`AM-presence` and `T08-row8` both say channel presence — is this person on
WhatsApp and in this season's group — is **rendered on the record**. On the same
day, Brian struck `On WhatsApp` from the **board** as not a recruit field, and
the rebuilt `W2-01` carries no such row either.

So the question `W3` used to absorb is now live and belongs to `W2`: does channel
presence appear on the recruit's record at all, and if so, where? It is recorded
against `W2` and is not answered here.

## Brian approval

- Exact words: _"W3 doesn't seem to hold anything. It should just be flat-out
  removed. Don't renumber anything."_ and _"At least there are no screens
  associated with it. It seems to have rolled into something else."_
- Date: 2026-08-31
