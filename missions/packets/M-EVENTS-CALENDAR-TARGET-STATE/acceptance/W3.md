# Acceptance — W3

- **Workflow:** `W3` — Load and correct a term's events by import
- **Screen IDs:** `W3-01` bulk import, empty season · `W3-02` bulk import,
  season with events · `W3-03` the confirmation table · `W3-04` where bulk
  import lives · `W3-05` refusals and boundaries
- **Grounding:** screenshots for `W3-04`, which changes the existing Events
  page — the current side carries the Playwright capture of `main` at
  `2072ecd`. The import surfaces are code-only and correctly so: there is no
  import, no export and no CSV handling on `main`.
- **Verdict:** approved
- **Brian's exact words:** "Other than that, I think this looks good. This is
  approved."
- **Date:** 2026-08-21
- **Open feedback:** none. Nineteen items were raised across two rounds and all
  were applied. The last, consistency of the period control at 375, was applied
  without a further review round at Brian's instruction.
- **What the two correction rounds were:**
  1. Flavour text that said nothing, a summary column that hid the changes
     instead of showing them, and row numbers nobody can interpret.
  2. An **ungrounded Events page** — this intake invented one and marked it a
     new surface while the real page sat in a screenshot in the same directory.
     Recorded because it is the same failure the mission's own history warns
     about, in smaller form.
- **Carried into the packet, not defects here:**
  - **D35 versus Brian.** The approved brief requires mass delete; Brian doubts
    it will be supported. `W3` settles only that no deletion reaches the system
    through a CSV. The conflict is carried to `W4`.
  - There is **no delete path on `main` at all** — an event cannot currently be
    removed by any means. `W4` has to specify one.
- **Evidence paths:**
  - `workflows/W3-load-and-correct-a-terms-events-by-import.md`
  - `mockups/W3-load-and-correct-a-terms-events-by-import.html`
  - `mockups/current/list-{desktop,phone}.png`
