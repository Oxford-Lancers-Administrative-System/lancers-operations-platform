# Acceptance — W8

- **Workflow:** `W8` — Administer event-type templates
- **Screen IDs:** `W8-01` seven types, seven templates · `W8-02` one template ·
  `W8-03` what the change will touch · `W8-04` the edges
- **Grounding:** code-only, and correctly so — there is no template, no per-type
  default and no admin surface anywhere on `main` at `2072ecd`.
- **Verdict:** approved
- **Brian's exact words:** "All right, looks good."
- **Date:** 2026-08-21
- **Open feedback:** none.
- **The two things this workflow changed about the mission:**
  1. **D41 refined to per-field inheritance.** A template change reaches only
     fields nobody has edited. Applied to the whole record it would overwrite a
     description an operator wrote by hand — the same class of failure as an
     amendment discarding answers. Proposed as an unapplied clarification.
  2. **The chase threshold was removed as Mission 4's.** It had been placed on
     the template in error; the Events brief records D75 but hands it to Task 03
     in its §7 and excludes reminder scheduling in its §8. The correction was
     chased through `W5` and `01-overview.md` as well.
- **Evidence paths:**
  - `workflows/W8-administer-event-type-templates.md`
  - `mockups/W8-administer-event-type-templates.html`
  - `notion-corrections.md`
