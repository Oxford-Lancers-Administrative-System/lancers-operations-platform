# Acceptance — W6

- **Workflow:** `W6` — Cancel an event
- **Screen IDs:** `W6-01` the cancellation · `W6-02` a cancelled event keeps its
  history · `W6-03` the silent path and the one-way door
- **Grounding:** code-only, and correctly so — nothing on `main` at `2072ecd`
  can reach the `cancelled` status.
- **Verdict:** approved
- **Brian's exact words:** "this looks pretty great" and "canceled for now.
  That's it."
- **Date:** 2026-08-21
- **Open feedback:** none.
- **The one open decision, closed:** `cancelled` is the only state and there is
  no `paused`. A known new date is `W5`'s reschedule; an unknown future is a
  cancellation plus a later new event; an unconfirmed event is a draft. The
  brief's existing notification handling (D58, D59, D76) stands unchanged.
- **Cross-workflow answer recorded here:** a cancelled event stays in the
  subscription feed marked cancelled (`W2`), stays on the public and operator
  lists marked cancelled (D57, and `W1` correction C1), and may reach a
  subscriber hours late — which is why the message, not the calendar, is how
  people find out.
- **Evidence paths:**
  - `workflows/W6-cancel-an-event.md`
  - `mockups/W6-cancel-an-event.html`
